name: Deploy to Server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Checkout source code
        uses: actions/checkout@v2

      - name: Installing dependencies
        run: docker ps

      - name: Run
        run: docker ps vite --host
