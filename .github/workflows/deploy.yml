name: Deploy to Server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Checkout source code
        uses: actions/checkout@v2

      - name: Installing dependencies
        run: yarn install

      - name: Run
        run: yarn run dev --host
